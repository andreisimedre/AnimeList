query AnimeListQuery($perPage: Int, $page: Int) {
  Page(perPage: $perPage, page: $page) {
    pageInfo {
      currentPage
      hasNextPage
    }
    media {
    ...AnimeDetails
    }
  }
}

fragment AnimeDetails on Media {
    id
    averageScore
    siteUrl
    title {
    english
    native
    }
    description
    bannerImage
    coverImage {
    large
    }
}


